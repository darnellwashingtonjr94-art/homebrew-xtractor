name: Test Homebrew Formula

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-formula:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Audit Formula
        run: brew audit --type=formula ./sandb0x-xtractor.rb || true

      - name: Install Formula
        run: brew install --build-from-source ./sandb0x-xtractor.rb
